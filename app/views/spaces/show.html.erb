<div class="container" style="margin-top:100px">
<!--Temporary solution. Detailed solution for this problem described here
http://stackoverflow.com/questions/10336194/twitter-bootstrap-top-nav-bar-blocking-top-content-of-the-page
-->
  <p id="notice"><%= notice %></p>
  <div class="row">
    <div class="col-sm-12 col-md-12"><h1><%=@space.title%>
      <% if current_user.present? %>
        <% unless current_user.favorites.exists?(space: @space) %>
          <%= link_to favorites_path(space_id: @space), method: :post do %>
            <span class="glyphicon glyphicon-star-empty"></span>
          <% end %>
        <% else %>
          <%= link_to favorite_path(@space), method: :delete do %>
            <span class="glyphicon glyphicon-star"></span>
          <% end %>
        <% end %>
      <% end %>
      </h1>
      </div>
  </div>

  <br>
  <br>
  <div class="row">
    <%= render 'bookings/form' %>
    <%= render 'map' %>
  </div>
  <div>
    <%= render 'description' %>
  </div>
  <div class="row">
    <%= render 'reviews/reviews' %>
    <%= render 'owner' %>
  </div>
</div>
